{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "story-arc",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "exec .venv/bin/uvicorn app:app --app-dir backend --port ${PORT:-8000}"],
      "port": 8000,
      "autoPort": true
    }
  ]
}
